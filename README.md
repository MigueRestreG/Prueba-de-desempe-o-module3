# CRUDTASK - Academic Performance Management System

A robust **CRUD** (Create, Read, Update, Delete) web application built to manage academic tasks and performance. This project features a clean, responsive interface with role-based access and user authentication.

## 🚀 Features

- **Authentication System**: Secure Login and Registration flow.
- **Admin Dashboard**: Global overview of all tasks, system-wide metrics, and administrative controls.
- **User Profiles**: Personalized profile pages with status badges and department information.
- **Task Management**: Full task lifecycle (creation, filtering by status, and priority management).
- **Responsive Design**: Mobile-first architecture that adapts from small smartphones to ultra-wide displays.
- **Role-Based Access**: Specialized views for standard users and administrators.

## 📁 Project Structure

```text
Prueba_De_Desempeno/
├── public/
│   └── views/            # HTML Views (Login, Admin, Tasks, etc.)
├── src/
│   ├── assets/           # UI Icons and Brand Logos
│   ├── services/         # JavaScript Logic (Auth, API calls, Guards)
│   └── styles/           # Component-specific CSS modules
├── db.json               # Local Mock Database (JSON Server)
└── package.json          # Project Dependencies and Scripts
```

## 🛠️ Setup & Installation

Follow these steps to get the project running locally:

1. Clone the Repository: git clone https://github.com/MigueRestreG/Prueba-de-desempe-o-module3.git

cd Prueba-de-desempe-o-module3

2. Install Dependencies: npm install

3. Run the API (Mock Server): npm run api

4. Run the Development Server: npm run dev
